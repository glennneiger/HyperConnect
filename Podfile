target :FlickerImageSlideViewer do
    use_frameworks!
    platform :ios, '9.0'

    pod 'Alamofire', '~> 4.0'
    pod 'AlamofireImage', '~> 3.0'

    target :FlickerImageSlideViewerTests do
        inherit! :search_paths
    end
end
